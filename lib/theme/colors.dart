import 'package:flutter/widgets.dart';

class CustomColors extends Color {
  const CustomColors(super.value);

  static const primary = Color(0xFF00DAE7);
  static const primaryDark = Color(0xFF0079A8);

  static const grey = Color(0xFF3D3D3D);
  static const darkGrey = Color(0xFF2B2B2B);

  static const background = Color(0xFF1F1F1F);

  static const offwhite = Color(0xFFEDF2F4);

  static const dark = Color(0xFF111111);
}
